@model IEnumerable<CompleteExampleApp.Models.Entities.Tag>
@{
    ViewData["Title"] = "Tags";
}

<h2>Tags</h2>

<p>
    <a class="btn btn-primary" asp-action="Create">Create New</a>
</p>

<table class="table table-striped table-bordered align-middle">
    <thead class="table-dark">
        <tr>
            <th>Name</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var t in Model)
        {
            <tr>
                <td>@t.Name</td>
                <td>
                    <a class="btn btn-sm btn-warning" asp-action="Edit" asp-route-id="@t.Name">Edit</a>
                    <a class="btn btn-sm btn-info" asp-action="Details" asp-route-id="@t.Name">Details</a>
                    <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@t.Name">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
