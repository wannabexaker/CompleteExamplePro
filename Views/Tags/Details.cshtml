@model CompleteExampleApp.Models.Entities.Tag
@{
    ViewData["Title"] = "Tag Details";
}

<h2>Tag Details</h2>

<div class="card mt-3">
    <div class="card-body">
        <h5 class="card-title">@Model.Name</h5>
    </div>
</div>

<a asp-action="Edit" asp-route-id="@Model.Name" class="btn btn-warning mt-3">Edit</a>
<a asp-action="Index" class="btn btn-secondary mt-3">Back</a>